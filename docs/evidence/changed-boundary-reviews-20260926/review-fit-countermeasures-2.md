LAND WITH FIXES

Independent read-only review by fit-review, 2026-09-26. Round 2 D/E implementation against main 0917aac. Fix F1 before declaring the E recipe or running it. D is acceptable as implemented; E is genuinely closed-loop, but its sampled endpoint can silently shorten the declared rollout.

Reviewed LF pins: policy/range_bc/train.py 693f12a60cdf14fa29c321c95c56d23075b03e50a2af337d6e9043d19507890b; tests/test_range_bc_torch.py 85ee15007c9e107f3d7a2e036d596f3b5ac8aad1e0567d1d9c3d8827c5da6aff. Both match. Hand-back 1117a6c9 and brief 99c96599 also match. Only those two code files were diff-reviewed; dependencies and proof artifacts were read to verify their behavior. No checkout edits, commits, Linear writes, game input, Mac jobs or sealed session reads.

1. **F1 — blocking before the E experiment: the start sampler includes an endpoint that cannot supply K feedback inputs.** `policy/range_bc/train.py:457` samples through `T-K` inclusive, while `:443` generates only through `T-2`, writing each result at `t+1`. For the proposed T=96, K=32, start=64 produces 31 decoded inputs (65..95), not 32. This is reachable: a fresh torch.Generator seeded with 0 draws 64 from randint(32,65). For the accepted K=1, start=95 generates no decoded input at all. Independent direct probes returned replacement counts 32, 32, 31, 0 for (start,K)=(32,32),(63,32),(64,32),(95,1). `tests/test_range_bc_torch.py:930` accepts the same clipping and does not test the wrapper's sampled endpoint.

   Expected fix: for a contract of K replacement inputs at s+1..s+K, restrict a full-window start to s <= T-K-1, handle impossible/oversize K explicitly, and update SELF_ROLL_RULE and the hand-back's bounds. Add a test forcing the maximum sampled start and asserting the declared count, including K=1. Alternatively explicitly change the declared recipe to a clipped-length distribution and report its effective length; do not describe the current sampler as always providing K steps. This does not invalidate its closed-loop decode on the steps it actually executes.

2. **N1 — note: the supplied E kill check is weaker than its description; a stronger independent mutation does fail.** `countermeasures/mutation_r2.py:13-16` first runs the correct rollout, then overwrites its returned segment with the true history. It tests discarding the result, not computing model outputs open-loop on true history. I separately changed the recurrent call from `pv[:, None]` to `prev[:, t:t+1]` in memory while retaining the decoded outputs; the existing closed-loop reference test failed with AssertionError. Replacing idle_vector with prev_vector(None) also failed with AssertionError. These were assertion failures, not collection/import failures. Improve the retained mutation script to exercise the true-history-fed model call and to require an assertion/test-failure exit rather than treating every nonzero pytest exit as a kill. This evidence gap is closed for this review by the stronger independent check.

3. **N2 — note: B seed 0 supports saturation as a mechanism, not a proved training-time cause or remedy.** The supplied probe code and JSON support the reported feature magnitude (28.97 mean, 738.32 max), near-constant hidden state (mean row std 0.004915), and near-constant outputs. The large, varying crop embeddings rule out an entirely dead encoder. However, `countermeasures/b0_probe2.py:20` measures W_ih*x plus biases, omitting W_hh*h(t-1), so its 98.95–99.58% figure is input-side preactivation magnitude, not the measured fraction of actual recurrent gates saturated. It performs no backward pass. Also :11 uses 1,024 linspace-sampled rows as a new sequence, rather than replaying every intervening frame. The inference is plausible and strong enough to flag an optimization pathology; it cannot establish when it arose, exclude seed-dependent failure, prove gradients were zero, or establish that normalization/lower LR will cure it. Temper the hand-back's categorical causal/remedy language. No rerun or new architecture is requested here.

Accepted checks:

- **D / known idle:** idle_vector uses zero hold/press/release, yaw ZERO_CLASS, pitch ZERO_CLASS only when the session has pitch gain, and known=1. It equals the executor/prev_vector idle reference. The corruption is applied after previous-action dropout, independent of labels, with one contiguous run per selected sequence and clipping at the window end. An independent active-history probe replaced 120 positions with known-idle vectors, not unknown vectors. Targets and masks are not changed. All-zero camera one-hot fields for unknown pitch do not make the overall history unknown.
- **E / feedback and leakage:** the no-grad prefix initializes state; then both recurrent state and the just-decoded previous-action vector are carried step by step. The reviewed live mask, tap/hold transition, saturated camera and exact median rules are reused. Perturbing the true previous-action inputs after the rollout start leaves the generated segment identical. The rollout output requires no gradient; the separate trained pass still gives frame-encoder gradients. No current targets or dev batches enter history generation; fit still refuses non-train sessions. Returning to teacher history after the declared segment is part of this windowed recipe, not a hidden reset inside it.
- **Exclusivity:** all three pairs among self_condition, idle_corruption and self_roll are rejected by fit, and run_fit applies the same guard before dataset/output work. Ordinary prev_dropout remains allowed; it precedes the selected option as declared.
- **Default off:** no new generator or history path runs at zero. The old own_previous loop's extracted operations retain their order. A separate small synthetic 4-step retrain executed both git-show 0917aac train.py and the worktree module: checkpoint bytes and epoch logs excluding seconds matched for both frame+history and history-only models. SHA256s were 77b02d02e842a82ab890f28fb294764cd213112d5da028dc3e0643ff82fff0d2 and deb9620f3298cd2a4996ade23e7909048f158dda0f6e9a84d88c53061213099c respectively. Additional small 96-step-window training probes confirmed both options are reproducible at p=.5, differ from off with history enabled, and equal off with history=False.

Proof audit:

- **(a)** repro_metrics.py hashes the loaded checkpoints, recomputes evaluation and compares canonical JSON for existing blocks and gates. r2-repro-metrics.json has all five comparisons true and SHA256 8a13a3fd20057a4d9289c732c9897439665f6f7076ca8e5ec9dfb016b28925b9, matching the retained arm-A re-read pin. This is report-block equality, not whole report-file equality.
- **(b)** r2-proof-b-receipt.json matches f7d2a6205826ca820a807358a9d957758b967e71883f4b4a1916d71c44523dfa. The comparison script checks actual old/new checkpoint files against both report entries: 5cb5a188... and 6a0cce0a... match. All recorded metric, loss-log, statistics and AR2 comparisons are true. The receipt honestly retains all_equal=false because it strips only two config keys. r2-config-check.txt reports equality after stripping exactly prev_dropout, self_condition, g1_press_source, idle_corruption and self_roll, with the stated values; source inspection agrees (only the final two keys are new relative to 0917aac). I audited these retained producer artifacts; I did not rerun the Mac corpus retrain or independently access its underlying report files. Prefer a revised retained comparison receipt listing all five exclusions to make the exception executable in one place. Default checkpoints/old report blocks are compatible; the entire report JSON is intentionally not byte-identical.

Local validation: six focused unmodified pytest tests passed (idle-vector, corruption run, closed-loop reference, executor parity and both median-boundary checks; 37 deselected). Independent in-memory probes covered the stronger E mutation, D unknown mutation, no mid-window true-history dependence, no-grad, trained frame gradients, all pairwise guards, default-vs-main bytes, and enabled/off training controls. A broad local torch-file run was deliberately stopped after eight passing tests to avoid spending PC resources repeating unchanged coverage; it is not claimed as a completed suite. Producer's retained r2-tests.log reports 190 passed, 1 skipped on the Mac. The independent endpoint probes expose F1 despite those passing tests.
