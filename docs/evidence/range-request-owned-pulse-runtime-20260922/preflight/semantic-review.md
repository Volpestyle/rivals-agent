# Lead runtime semantic decision: request-start-owned-pulse-v1

The lead accepts one exploratory normal-resource Luna setup run using unchanged request checkpoint6ee38807 and threshold0.7 after independent software acceptance676f99a. This is a new runtime execution interpretation. The source label forecasts a fresh received RMB point within100ms; it does not supervise completing a hold inside that horizon. Immediate guarded dispatch is a deliberate action choice, not copying the source97.984ms lead or physical motor behavior.

An accepted Controller start atA has immutable nominal endE=A+33ms. Actual first nonneutral commit must precede original requestD, original resource observation+100ms and effectiveH=min(E,phaseend,absolute sessionend). Same-owner continuation may followD but must precedeH; delays consume E, never renew it. Fault/refusal/loss/target change or explicit exit cancels. Not-sent/failed inputs consume the owner; confirmed recorded cancellation permits later fresh requests. A returned-afterH call remains recorded as returned then immediately released. Physical holds and emission timing remain measured outcomes, not software guarantees.

Historical scripted D independently confirmed two owned LT Web-Cluster casts/impacts on Luna in the matching saved PAD setup. That evidence supports primitive identity and the nominal33ms calibration reference; it does not establish equivalence of the newly extended held-input authority. The already audited learned runs also show this primitive. No repeat calibration is necessary merely to test the explicitly chosen software interpretation.

Source MK vs runtime PAD readers, all13 feature/known pairs, offline grid vs actual acquisition/processing, source window resets vs continuous generic selector/camera compensation, local target IDs and unknown original motor settings remain explicit uncertainties. One source event/four negatives is TRAIN-only; this run cannot establish useful generalization, pro play or the designated-Galacta matched benchmark. Luna is a fresh setup restriction, not an enforced named-target recognizer.

One named run: <=10s learned phase, <=14s camera startup under absolute24s session authorization, no scoreboard, no threshold/thread/model tuning or rerun to select success. Every proposal/refusal/acceptance/send/release and original video is retained. Fresh effective normal-resource menu, PAD/LT-web layout, range/Spider-Man/Luna appearance and process focus are required at entry; current saved values alone do not prove them. This decision only becomes bound once those real inputs and reviewed deployed manifests exist. Old source labels/models/recordings/receipts remain unchanged.
